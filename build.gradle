plugins {
    id 'java'
    id 'maven-publish'
}

allprojects {
    repositories {
        mavenCentral()
        maven { url 'https://jitpack.io' }
    }
    group = 'com.github.taes-k'
    version = '0.0.7'
}

subprojects {
    apply plugin: 'java'
    apply plugin: 'maven-publish'
}
