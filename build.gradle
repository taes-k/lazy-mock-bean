plugins {
    id 'java'
    id 'maven-publish'
}

allprojects {
    repositories {
        mavenCentral()
        maven { url 'https://jitpack.io' }
    }
    group = 'io.github.lazymockbean'
    version = '0.0.8'
}

subprojects {
    apply plugin: 'java'
    apply plugin: 'maven-publish'
}
